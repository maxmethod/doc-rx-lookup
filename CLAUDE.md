# CLAUDE.md — rx-provider-lookup

## Where knowledge lives — read this before writing a memory file

**The brain is the Obsidian vault: `~/Desktop/Marshall Wiki v1`** (private repo
`maxmethod/marshall-wiki`). This repo holds code. Durable knowledge does not live here
and does not live in the auto-memory directory.

- Live project status → vault `04-Active Projects/<project>.md`
- Platform/API knowledge → vault `05-Reference/`
- **Before ANY GHL API write, delete, or bulk op → vault `05-Reference/GHL/_HAZARDS.md`**
- Cross-project facts → vault `claude/Memories.md`

The auto-memory directory is a **ROUTER ONLY**. Do not create new topic files there. If
you catch yourself writing one, stop and move the content into the vault in the same turn.

**PHI never enters git. Ever.** No client person-data in this repo, the vault, or any
pushed doc. Client data lives only in `client projects/<client>/`.
