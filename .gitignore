# Source data files — not tracked; regenerated by scripts/build-zip-dataset.js
US.txt
2024_Gaz_zcta_national.txt
*.zip

# Editor / OS
.DS_Store
.vscode/
.idea/
*.swp

# Node (in case we add tooling later)
node_modules/

# Local dev conveniences
.env
.env.local
*.log

# Preview server artifacts
.claude/

# Local embed tester (not meant for production)
test-embed.html

# macOS/iCloud/Drive copy-conflict artifacts ("Foo 2.tsx", "[id] 2/"). Never part of a build, and a
# " 2" route dir breaks `next build`. gitignore syntax → these bare patterns match at any depth.
* [0-9].*
* [0-9]
